"use client";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { ChartType } from "@/lib/api";
const colors = ["#60a5fa", "#a78bfa", "#2dd4bf", "#fbbf24"];
export function MetricChart({ data, type, xKey, series }: { data: Record<string, string | number>[]; type: ChartType; xKey: string | null; series: string[] }) { const yKey = series[0]; if (!data.length || !yKey) return <div className="grid h-72 place-items-center text-sm text-slate-500">No data available</div>; const common = <><CartesianGrid stroke="#243043" strokeDasharray="3 3" vertical={false}/><XAxis dataKey={xKey ?? undefined} stroke="#64748b" tickLine={false} axisLine={false} fontSize={12}/><YAxis stroke="#64748b" tickLine={false} axisLine={false} fontSize={12}/><Tooltip contentStyle={{ background: "#0f172a", border: "1px solid #334155", borderRadius: 8 }} /></>; if (type === "pie") return <ResponsiveContainer width="100%" height={300}><PieChart><Tooltip contentStyle={{ background: "#0f172a", border: "1px solid #334155" }}/><Legend/><Pie data={data} dataKey={yKey} nameKey={xKey ?? undefined} innerRadius={62} outerRadius={100} paddingAngle={4}>{data.map((_, i) => <Cell key={i} fill={colors[i % colors.length]}/>)}</Pie></PieChart></ResponsiveContainer>; if (type === "line") return <ResponsiveContainer width="100%" height={300}><LineChart data={data}>{common}<Line type="monotone" dataKey={yKey} stroke="#60a5fa" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer>; if (type === "area") return <ResponsiveContainer width="100%" height={300}><AreaChart data={data}>{common}<defs><linearGradient id="fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#60a5fa" stopOpacity={.45}/><stop offset="100%" stopColor="#60a5fa" stopOpacity={0}/></linearGradient></defs><Area type="monotone" dataKey={yKey} stroke="#60a5fa" strokeWidth={2} fill="url(#fill)"/></AreaChart></ResponsiveContainer>; return <ResponsiveContainer width="100%" height={300}><BarChart data={data}>{common}<Bar dataKey={yKey} fill="#60a5fa" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer>; }
