import { BarChart3 } from "lucide-react";
export default async function SectionPage({ params }: { params: Promise<{ section: string }> }) { const { section } = await params; const title = section.charAt(0).toUpperCase() + section.slice(1); return <div className="grid min-h-[65vh] place-items-center"><div className="max-w-md text-center"><span className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-blue-500/15 text-blue-300"><BarChart3/></span><h1 className="mt-5 text-2xl font-semibold">{title} workspace</h1><p className="mt-3 text-sm leading-6 text-slate-400">This module is ready to connect to curated Cube semantic models and saved analysis views.</p></div></div>; }
