import { ArrowDownRight, ArrowUpRight } from "lucide-react";
export function KpiCard({ label, value, change, positive = true }: { label: string; value: string; change: string; positive?: boolean }) { return <article className="rounded-xl border border-slate-800 bg-slate-900/50 p-5"><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-2xl font-semibold tracking-tight text-slate-100">{value}</p><p className={`mt-3 flex items-center gap-1 text-xs ${positive ? "text-emerald-400" : "text-rose-400"}`}>{positive ? <ArrowUpRight size={14}/> : <ArrowDownRight size={14}/>} {change} <span className="text-slate-500">vs. last month</span></p></article>; }
